services:
  lalune-panel:
    build: .
    image: lalune-panel
    container_name: lalune-panel
    restart: unless-stopped
    ports:
      - "6333:6333"
    volumes:
      - ./data:/data
    environment:
      LALUNE_DATA_DIR: /data
      LALUNE_LISTEN: ":6333"
